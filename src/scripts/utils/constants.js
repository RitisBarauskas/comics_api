const nav = document.querySelector('.card__nav');
const firstButton = nav.querySelector('.card__button_first');
const prevButton = nav.querySelector('.card__button_prev');
const randButton = nav.querySelector('.card__button_rand');
const nextButton = nav.querySelector('.card__button_next');
const lastButton = nav.querySelector('.card__button_last');

export {firstButton, prevButton, randButton, nextButton, lastButton};